---
# Common Role Handlers

- name: Restart docker
  systemd:
    name: docker
    state: restarted
    daemon_reload: yes

- name: Reload docker daemon
  systemd:
    name: docker
    state: reloaded

- name: Restart snapd
  systemd:
    name: snapd
    state: restarted
