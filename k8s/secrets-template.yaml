---
# Committable secrets template for CI validation.
# Replace placeholder values for real deployments.

apiVersion: v1
kind: Secret
metadata:
  name: mysql-credentials
  namespace: chatapp
type: Opaque
stringData:
  mysql-root-password: "REPLACE_ME"
  mysql-user: "REPLACE_ME"
  mysql-password: "REPLACE_ME"

---
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: chatapp
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_ME"
  FRONTEND_URL: "http://REPLACE_ME:30080"

---
apiVersion: v1
kind: Secret
metadata:
  name: frontend-secrets
  namespace: chatapp
type: Opaque
stringData:
  VITE_BACKEND_URL: "http://REPLACE_ME:30080"
